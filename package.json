{
  "name": "node-express-ts-lambda",
  "version": "1.0.0",
  "description": "a node, express, and typescript server deployed to aws lambda and tested using jest. using this as template for new project",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/express": "^4.17.18",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "rm -r dist && tsc && node dist/app.js",
    "dev": "ts-node src/app.ts",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/supertest": "^2.0.12",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
