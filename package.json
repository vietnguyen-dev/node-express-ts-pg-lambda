{
  "name": "node-express-ts-pg",
  "version": "1.0.0",
  "description": "a node, express, typescript server that connects to a pg database and tested using jest. to be forked for future projects. can deploy anywhere",
  "main": "dist/server.js",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "scripts": {
    "start": "rm -r dist && tsc && node dist/server.js",
    "dev": "ts-node src/server.ts",
    "test": "jest"
  },
  "devDependencies": {
    "@types/express": "^4.17.18",
    "@types/jest": "^29.5.5",
    "@types/pg": "^8.10.3",
    "@types/supertest": "^2.0.12",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
